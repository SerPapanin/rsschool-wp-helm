apiVersion: v1
kind: Service
metadata:
  name: wordpress-mysql
  namespace: {{ .Values.namespace }}
  labels:
    app: wordpress-mysql
spec:
  ports:
    - port: {{ .Values.mysql.port }}
  selector:
    app: wordpress-mysql
  clusterIP: None
